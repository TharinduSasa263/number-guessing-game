# Number Guessing Game

A terminal-style browser game where you guess a number between 1 and 100. The game uses `window.prompt()` for input and `window.alert()` for feedback, keeping track of how many attempts you take.